@model IEnumerable<BikeStores.Models.Staff>

@{
    ViewData["Title"] = "Staff";
}

<div class="page-header">
    <h1>Staff</h1>

    <form method="get" class="mb-2">
        <div class="input-group">
            <input type="text" name="search" class="form-control" placeholder="Search by first name or last name..." value="@ViewBag.Search" />
            <button type="submit" class="btn btn-primary">Search</button>
            <a asp-action="Index" class="btn btn-secondary">Clear</a>
        </div>
    </form>
</div>

@if (!Model.Any())
{
    <p class="text-muted">No staff found.</p>
}
else
{
    <table class="table table-striped table-hover">
        <thead class="table-dark">
            <tr>
                <th>First Name</th>
                <th>Last Name</th>
                <th>Email</th>
                <th>Phone</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.FirstName</td>
                    <td>@item.LastName</td>
                    <td>@item.Email</td>
                    <td>@item.Phone</td>
                    <td>
                        <a asp-action="Details" asp-route-id="@item.StaffId" class="btn btn-sm btn-info">Details</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
